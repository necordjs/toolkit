version: '3.7'

services:
  necord-service:
    container_name: 'necord-service'
    restart: unless-stopped
    image: 'ghcr.io/necordjs/toolkit:latest'
    environment:
      DISCORD_TOKEN: 'your-token'
